# HackerNews CLI
HackerNews CLI fetches the top 10 stories from HackerNews and displays them in the terminal. Built for resolution.hackclub.com.

## Building
Openssl development libraries are required for building e.g. `openssl-devel` on fedora. 
